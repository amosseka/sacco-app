//Init

let firstDate = document.querySelector('input[name=first_date]');
let secondDate = document.querySelector('input[name=second_date]');

firstDate.setAttribute('type', 'date');
secondDate.setAttribute('type', 'date');